# WFW430 Control Center — Audit (المرحلة 1)

تم بناء هذا الـ Audit بعد قراءة كامل الملفات الفعلية (index.html, styles.css, app.js, sw.js, manifest.webmanifest, README.md) وتشغيل فحوصات آلية حقيقية:
- `node --check app.js` → لا يوجد أخطاء Syntax.
- مطابقة كل `getElementById` في app.js مع الـ id الموجودة في index.html (37 استدعاء) → لا يوجد أي id مفقود، ولا يوجد id مكرر في HTML.
- فحص توازن الوسوم في index.html بمحلل HTML كامل → لا يوجد وسم غير مغلق أو غير متطابق.
- فحص توازن `{}` و`()` في styles.css → متوازن (197/197، 91/91).
- فحص manifest.webmanifest كـ JSON → صالح لكنه بدون مفتاح `icons`.

كل بند أدناه مرتبط بملف وسطر فعلي، وليس افتراضًا.

---

## Critical

### C1 — Service Worker: أي تحديث قادم قد لا يصل للمستخدمين إطلاقًا
**الملف:** `sw.js` كامل (12 سطر)
إستراتيجية fetch الحالية cache-first بحت: `caches.match(e.request).then(r=>r||fetch(e.request))` — أي ملف موجود في الكاش لا يُعاد التحقق منه من الشبكة أبدًا. الحماية الوحيدة من التخزين القديم هي تغيير الاسم `const CACHE='wfw430-v2'` يدويًا. لو نُشر تحديث على GitHub Pages وتم نسيان تغيير هذا السطر، المتصفح لن يكتشف أي تحديث في sw.js نفسه (لأن محتواه لم يتغير) فلن تُشغَّل حتى دورة install/activate الجديدة — يبقى المستخدم عالقًا على نسخة قديمة إلى الأبد. هذا بالضبط ما طلبت التأكد منه في البند 6.
**الأثر:** كل إصلاح ننجزه بالمرحلة 2 قد لا يظهر فعليًا للموظفين.

### C2 — `structuredClone` بدون أي fallback داخل معالج الأخطاء نفسه
**الملف:** `app.js` أسطر 33-40 (`loadState`)، وأيضًا أسطر 224 و223 (`resetBtn`, `importBackupInput`)
```js
try{
  ...
  if(!raw) return structuredClone(seed);
  ...
}catch{ return structuredClone(seed); }
```
لو `structuredClone` غير مدعوم (متصفح/WebView قديم)، الاستدعاء الأول يفشل ويدخل `catch`، لكن `catch` نفسه يستدعي `structuredClone` مرة ثانية — فيفشل مرة ثانية لكن هذه المرة بدون أي حماية، فيتوقف تحميل الصفحة بالكامل من أول سطر تشغيل.
**الأثر:** انهيار كامل للتطبيق (شاشة بيضاء) على أي متصفح/WebView لا يدعم الدالة، بدون أي رسالة خطأ للموظف.

### C3 — تصدير PDF على أندرويد قد يُصدّر الصفحة الخطأ
**الملف:** `app.js` أسطر 202-216 (`printView`)
```js
setTimeout(()=>{
  window.print();
  document.title=oldTitle;
  if(name!==previous) switchView(previous);
},80);
```
على Chrome لسطح المكتب `window.print()` غالبًا يوقف تنفيذ الكود لحين إغلاق نافذة الطباعة. لكن على أندرويد (المنصة الأساسية لهذا النظام حسب طلبك) يفتح `window.print()` طبقة المشاركة/الحفظ كـ PDF الخاصة بالنظام بشكل **غير متزامن** — يعني السطرين اللي بعده (`switchView(previous)`) ينفذان فورًا بينما شاشة "حفظ كـ PDF" لسا تُجهَّز، فقد يلتقط أندرويد لقطة الصفحة وهي في حالة انتقال (عرض سابق بدل التقرير المطلوب) أو صفحة فارغة.
**الأثر:** بالضبط النقطة اللي وصفتها بأنها "مهمة جدًا" (تصدير PDF) قد تفشل تحديدًا على أندرويد.

### C4 — لا يمكن تثبيت التطبيق كأيقونة على الشاشة الرئيسية
**الملف:** `manifest.webmanifest` (كامل الملف، 8 مفاتيح فقط، تحقق JSON ناجح)
لا يوجد مفتاح `icons` إطلاقًا، ولا يوجد أي ملف صورة في المشروع (لا PNG ولا SVG). متصفح أندرويد Chrome يشترط `icons` (192px و512px على الأقل) ليعرض خيار "إضافة إلى الشاشة الرئيسية" فعليًا. هذا يمس مباشرة الوحدة اللي وصفتها في README: "PWA بسيطة تعمل Offline بعد أول تحميل".
**الأثر:** لا يمكن تثبيت النظام كتطبيق على جوالات الموظفين — يبقى رابط متصفح فقط.

---

## High

### H1 — جدول "أداء الموظفين" باليوم التاريخي لا يطابق أرقام الملخص فوقه
**الملف:** `app.js` أسطر 102-122 (`renderDashboard`) + `activeEmployees()` سطر 80
مجاميع KPI بالأعلى (`totals`) تُحسب من كل سجلات اليوم `logs` بدون أي فلترة على حالة الموظف. لكن جدول "أداء الموظفين" تحته يُبنى فقط من `activeEmployees()` (الموظفين غير "غير نشط" **حاليًا**، وليس وقت التاريخ المختار). فلو موظف اشتغل يوم 1 رمضان مثلًا وبعدها انتقل لحالة "غير نشط"، أرقامه تبقى داخلة في KPI الإجمالي لكن صفه يختفي تمامًا من الجدول — يعني مجموع صفوف الجدول لن يساوي رقم KPI بالأعلى. هذا خلل حقيقي في منطق التقارير (وليس تغييرًا معماريًا)، ويظهر بوضوح في أي PDF يُرسل للإدارة.

### H2 — الصفوف قد تُقص بين صفحتي طباعة
**الملف:** `styles.css` قسم `@media print` (أسطر 48-85)
يوجد `break-inside:avoid` على `.card,.knowledge-card,.employee-card,.kpi` لكن **لا يوجد على `tr`**. طلبك رقم 4 كان صريحًا: "يمنع قص الصفوف بين الصفحات قدر الإمكان". حاليًا لا يوجد أي قاعدة تمنع ذلك على مستوى الصف.
(بالمقابل: تكرار رؤوس الجدول `<thead>` بكل صفحة **يعمل فعليًا بدون أي كود إضافي** — لأن الجدول Semantic صحيح (`thead`/`tbody`) و`.table-wrap` بالطباعة `overflow:visible` — هذي نقطة إيجابية موجودة مسبقًا، ما تحتاج تعديل.)

### H3 — أزرار القائمة على الجوال أصغر من الحد الموصى به لأندرويد
**الملف:** `styles.css` — `.icon-btn` (سطر 18) و`.sidebar-close` (سطر 31): كلاهما `width:38px;height:38px`
إرشادات Material Design (المرجع الرسمي لأندرويد) توصي بحد أدنى 48dp لمنطقة اللمس. الزرين الوحيدين للتحكم بالقائمة الجانبية على الجوال (☰ و×) أصغر من ذلك — أثر مباشر على البند 1 (Mobile First / Android) والبند 7 (touch target sizes).

### H4 — القائمة الجانبية لا تدير التركيز (focus) عند الفتح/الإغلاق
**الملف:** `app.js` أسطر 50-55 (`setMenu`) + `index.html` (الـ nav)
عند فتح الدرج على الجوال، التركيز لا ينتقل لأول عنصر فيه، والمحتوى خلف الـ backdrop لا يُعلَّم `aria-hidden`/`inert`، فمستخدم لوحة مفاتيح/قارئ شاشة قد "يتنقّل" لعناصر مخفية بصريًا خلف الطبقة الداكنة. كذلك عناصر `.nav-item` لا تحمل `aria-current` لتمييز القسم الحالي لقارئ الشاشة (البند 7: keyboard navigation, aria attributes).

---

## Medium — للاطلاع فقط، لن أنفذها الآن (تنتظر موافقتك لأنها تلامس طبقة البيانات/السلوك العام)

- **لا يوجد schemaVersion/آلية migration** في `state` (البند 8 بالضبط) — حاليًا `loadState()` يدمج البيانات المحفوظة فوق seed بدون رقم إصدار. يعمل تمام الآن، لكن أي تغيير مستقبلي بشكل البيانات يحتاج مكان يُبنى فيه. **أقترح فقط إضافة حقل `schemaVersion` بدون تغيير أي سلوك حالي — أنتظر تأكيدك قبل تنفيذه** لأنه يلامس طبقة التخزين.
- `alert('ملف النسخة الاحتياطية غير صالح.')` (app.js سطر 223) يستخدم نافذة المتصفح الافتراضية بدل نظام الـ toast المصمم أصلًا — غير متسق بصريًا. الـ `confirm()` الخاصة بالحذف مقصودة ومناسبة (تحذير قوي لإجراء حذف)، لا أقترح تغييرها.
- لا يوجد أي إشعار للمستخدم عند توفر نسخة جديدة من Service Worker (بعد إصلاح C1، يستحق إضافة toast "تحديث جديد، اضغط لإعادة التحميل").
- خط `"Segoe UI"` أول عنصر بقائمة الخطوط (styles.css سطر 8) لا يتوفر أصلًا على أندرويد؛ يعمل accord بفضل fallback لكن يستحق مراجعة قائمة خطوط أنسب للعربي على أندرويد.
- لا يوجد `<link rel="icon">` في `<head>` — تبعية مباشرة لإصلاح C4 (الأيقونات).
- Empty states موجودة وصحيحة وظيفيًا (تحققت من كل `colspan` مقابل عدد الأعمدة الفعلي — كلها مطابقة)، لكنها بصريًا نص فقط بدون أي لمسة (البند 2).
- خصائص CSS فيزيائية (`margin-left/right`) بدل المنطقية (`margin-inline-start/end`) — تحسين كود اختياري بدون أي أثر وظيفي لأن المشروع RTL فقط.

## Low
- دوال البحث (`renderCases`, `renderKnowledge`) تستخدم `JSON.stringify(x).toLowerCase().includes(q)` أي تفتش داخل كامل الكائن (بما فيه id/timestamp الداخلية) — نادرًا ما يسبب نتيجة خاطئة عمليًا، لا يستحق تعقيد الآن.
- أزرار `.mini-btn` داخل الجداول مضغوطة على الجوال — لكنها داخل جدول قابل للتمرير أفقيًا أصلًا، تعديلها الآن قد يُخل بكثافة الجدول التي طلبت إبقاءها بسيطة.

## نقاط تحققت أنها **سليمة فعلًا ولا تحتاج أي تعديل**
- كلمة "وزن": لا تظهر إطلاقًا في أي نص واجهة (لا index.html ولا مخرجات app.js) — فقط `name="salesWeight"` كاسم متغير داخلي غير ظاهر للمستخدم. لا حاجة لأي تغيير هنا.
- تباين الألوان (Contrast): تحققت حسابيًا من `--muted` على أبيض (≈4.97:1) وشارات `warn` (≈5.2:1) — كلاهما يجتاز WCAG AA للنص العادي.
- القائمة الجانبية على الجوال: مخفية افتراضيًا، تفتح من ☰ فقط، تُغلق بالنقر خارجها/بزر ×/بزر Escape — كل هذا موجود ويعمل منطقيًا بالكود الحالي.
- إغلاق الشفت: صيغة الحساب والحدود (0 / |فرق|≤1 / غير ذلك) مطابقة تمامًا لما وصفته ولما في README.
- لا تعارض بين رقم `colspan` وعدد أعمدة أي جدول (تحققت من الأربعة جداول + جدول الأداء).
- لا id مكرر، لا id مفقود، لا خطأ Syntax في JS، لا وسم HTML غير متطابق، لا خطأ في بنية CSS.

---

## ⚠️ حدود هذا الـ Audit (بصراحة)
أنا بيئة نصية بلا متصفح فعلي، فما يلي **لم يُختبر بصريًا على جهاز حقيقي** رغم أني راجعت كل قاعدة CSS المرتبطة به يدويًا ولم أجد سببًا واضحًا لحدوثه:
- عدم وجود Horizontal Overflow فعليًا على 360/390/412/430px.
- الشكل النهائي الفعلي للطباعة (PDF) على متصفح أندرويد حقيقي.

بعد تنفيذ Critical+High، أعطيك بالتقرير النهائي خطوات تحقق يدوية سريعة (أقل من دقيقتين) تأكد فيها هذي النقاط بنفسك على جهازك، لأنها الطريقة الوحيدة للتأكد الحقيقي منها.

---

## CACO Analyzer — مرحلة التنفيذ

- أضيف Parser مستقل لملفات XLSX/XLS/CSV/TSV مع اكتشاف صف العناوين وتطبيع المبلغ والتاريخ.
- أضيف Classification Engine مستقل؛ لا يعتمد على DOM أو localStorage ويمكن اختباره مباشرة.
- صفوف Total/Subtotal مستبعدة من عد العمليات ومحتفظ بها للتحقق الحسابي.
- Golden Fixture يثبت 25 صفًا ظاهرًا = 24 عملية + صف Total، ومجموع 2699.59.
- القيمة 882.95 لا تدخل أي شرط تصنيف.
- RS Setup Fee المنفرد يسجل Attribute يحتاج مراجعة ولا يدخل كعملية مستقلة.
- لا يوجد تجميع آلي على MSISDN أو الوقت.
- واجهة CACO Mobile-first تعرض أخطاء القراءة، نتائج التصنيف، ملخص الموظف والمعرض، وفلاتر المراجعة.
- Daily Tracking لا يستبدل سجلًا موجودًا من المصدر نفسه، والموظف غير المربوط يُستبعد.
- Shift Closing يُعبأ فقط ثم ينتظر مراجعة المستخدم والحفظ اليدوي؛ الأنماط غير المعروفة لا تدخل مبالغ الترحيل.
- لم يتغير schemaVersion: بيانات التحليل لا تُحفظ، والتكامل يستخدم حقول الإصدار 2 الموجودة.
- مكتبة SheetJS مضمّنة محليًا كي تبقى قراءة Excel متاحة في وضع PWA دون اتصال بعد التخزين الأول.
